From 3deb39111b4450aa4b6db564d7a02724c96e1577 Mon Sep 17 00:00:00 2001
From: Almar Klein <almar@almarklein.org>
Date: Wed, 18 Sep 2024 12:30:12 +0200
Subject: [PATCH] Use default min_binding_size

---
 pygfx/renderers/wgpu/engine/binding.py | 5 +----
 1 file changed, 1 insertion(+), 4 deletions(-)

diff --git a/pygfx/renderers/wgpu/engine/binding.py b/pygfx/renderers/wgpu/engine/binding.py
index d1a0cd7fd..82c59cd8c 100644
--- a/pygfx/renderers/wgpu/engine/binding.py
+++ b/pygfx/renderers/wgpu/engine/binding.py
@@ -87,16 +87,13 @@ def get_bind_group_descriptors(self, slot):
                     "size": resource.nbytes,
                 },
             }
-            # Note: we set min_binding_size, rather than relying on the default (None),
-            # otherwise the layout for different buffers look equal, and are thus
-            # re-used (using caching), but they won't be compatible.
             binding_layout = {
                 "binding": slot,
                 "visibility": self.visibility,
                 "buffer": {
                     "type": getattr(wgpu.BufferBindingType, subtype),
                     "has_dynamic_offset": False,
-                    "min_binding_size": resource.nbytes,
+                    "min_binding_size": None,
                 },
             }
         elif self.type.startswith("sampler/"):
